# Base URL of the recalls API (no trailing slash).
# Live now:  https://consumer-product-recalls-api.fly.dev
# Planned:   https://api.consumer-product-recalls.info
PUBLIC_API_URL=https://consumer-product-recalls-api.fly.dev

# Optional: Cloudflare Web Analytics token (cookieless). Analytics is off when unset.
# PUBLIC_CF_ANALYTICS_TOKEN=

